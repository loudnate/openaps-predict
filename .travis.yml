language: python
python:
    - "2.7"
before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -y python-scipy libatlas-dev liblapack-dev
install:
    python setup.py install
script:
    python setup.py test
